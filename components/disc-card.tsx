import Image from 'next/image';
import Link from 'next/link';
import type { Product } from '@/lib/types';
export function DiscCard({ artist }: { artist: Product }) { return <Link href={`/shop/${artist.slug}`} className="group block"><div className="relative aspect-square overflow-hidden rounded-full border border-line bg-ink-2"><Image src={artist.image} alt={artist.name} fill className="object-cover grayscale transition duration-700 group-hover:scale-105 group-hover:rotate-6 group-hover:grayscale-0" sizes="(max-width: 768px) 45vw, 20vw" /><div className="absolute left-1/2 top-1/2 h-10 w-10 -translate-x-1/2 -translate-y-1/2 rounded-full border border-paper/20 bg-ink/90"><span className="absolute left-1/2 top-1/2 h-2 w-2 -translate-x-1/2 -translate-y-1/2 rounded-full bg-signal" /></div></div><div className="mt-4 flex justify-between gap-3"><span className="font-display text-xl uppercase">{artist.name}</span><span className="pt-1 font-mono text-[9px] uppercase tracking-widest text-grey">{artist.genre}</span></div></Link>; }
