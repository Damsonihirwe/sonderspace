# SONDERspace

Music-driven streetwear: your favorite artist, your own tee.

## Run locally

```bash
npm install
npm run dev
```

Open http://localhost:3000.

The request endpoint is available at `POST /api/request`. Add a mail or WhatsApp provider inside that route when production credentials are available.
